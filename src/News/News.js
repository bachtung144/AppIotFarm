import React from 'react';
import {Image, Text, View, TextInput, Linking, StatusBar} from 'react-native';

const News = React.memo(function News() {
    return(
        <View>
            <Text>News</Text>
        </View>
    )
});

export default News;
